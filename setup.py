from setuptools import setup, find_packages

setup(
    name='df_first',
    version="0.1.0",
    description="sample of minimum package",
    author='Hiroto Minami',
    packages=find_packages(),  # 使うモジュール一覧を指定する
    license='MIT'
)